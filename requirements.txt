# PDF-cli - Dependências do Projeto
# Versão mínima Python: 3.8

# Manipulação de PDFs
PyMuPDF>=1.23.0  # Biblioteca principal para manipulação avançada de PDFs (fitz)
PyPDF2>=3.0.0    # Biblioteca auxiliar para operações complementares de PDF

# Processamento de Imagens
Pillow>=10.0.0   # Manipulação de imagens (filtros grayscale, invert)

# Conversão Markdown para PDF (Fase 9)
markdown2>=2.4.0  # Conversão de Markdown para HTML
weasyprint>=59.0  # Conversão de HTML para PDF (preferido, mas requer libs do sistema no Windows)
xhtml2pdf>=0.2.17  # Fallback para HTML->PDF (portável, funciona no Windows sem dependências externas)

# CLI e Interface
# argparse está incluído na biblioteca padrão do Python (não precisa instalar)
# Removido typer e rich para compatibilidade com terminais simples (CMD/PowerShell)

# Build e Distribuição (opcional, instalado automaticamente pelos scripts)
# PyInstaller>=5.0.0  # Gerador de executáveis standalone (instalado pelos scripts de build)
